<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Add Create Invoice button to Purchase Order -->
    <record id="view_purchase_order_form_inherit" model="ir.ui.view">
        <field name="name">purchase.order.form.inherit.invoice.wizard</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//button[@name='action_create_invoice']" position="after">
                <button name="%(action_purchase_make_invoice_advance)d" type="action" 
                        string="Create Invoice (Advanced)" 
                        class="btn-secondary"
                        attrs="{'invisible': [('state', 'not in', ['purchase', 'done'])]}"/>
            </xpath>
            <xpath expr="//field[@name='tax_totals']" position="after">
                <field name="amount_invoiced" readonly="1"/>
                <field name="amount_to_invoice" readonly="1"/>
            </xpath>
            <xpath expr="//field[@name='order_line']/tree//field[@name='price_subtotal']" position="after">
                <field name="amount_invoiced" readonly="1"/>
                <field name="amount_to_invoice" readonly="1"/>
            </xpath>
        </field>
    </record>

    <!-- Add action to purchase order list view -->
    <record id="action_purchase_invoice_advanced_list" model="ir.actions.act_window">
        <field name="name">Create Advanced Invoice</field>
        <field name="res_model">purchase.make_invoice_advance</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="context">{'active_ids': active_ids, 'active_model': 'purchase.order'}</field>
    </record>

    <!-- Add server action to create advanced invoices -->
    <record id="action_purchase_order_create_advanced_invoice" model="ir.actions.server">
        <field name="name">Create Advanced Invoice</field>
        <field name="model_id" ref="purchase.model_purchase_order"/>
        <field name="binding_model_id" ref="purchase.model_purchase_order"/>
        <field name="state">code</field>
        <field name="code">
action = {
    'type': 'ir.actions.act_window',
    'name': 'Create Invoice',
    'res_model': 'purchase.make_invoice_advance',
    'view_mode': 'form',
    'target': 'new',
    'context': {
        'default_purchase_order_ids': [(6, 0, env.context.get('active_ids', []))],
    }
}
        </field>
    </record>
</odoo>
