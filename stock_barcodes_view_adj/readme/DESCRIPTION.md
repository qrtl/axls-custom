This module adjusts the inventory barcode interface by removing the Apply button from inventory (stock count) operations.

This prevents inventory adjustments from being applied directly from the barcode interface. Counted quantities must instead be reviewed and applied from the Inventory backend.
